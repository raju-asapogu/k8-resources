apiVersion: v1
kind: Pod
metadata:
  name: service-demo
  # These labels are used to register this pod with service that we have created
  labels:
    project: roboshop
    component: frontend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx
