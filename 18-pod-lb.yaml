apiVersion: v1
kind: Pod
metadata:
  name: service-lb-demo
  # These labels are used to register this pod with service that we have created
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: load-balancer
spec:
  containers:
  - name: nginx
    image: nginx
