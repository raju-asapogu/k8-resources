apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  labels:
    # these labels are replicaSet labels
    purpose: dp-demo
    project: roboshop
spec:
  replicas: 4
  selector:
    # these labels are used to select pods
    matchLabels:
      purpose: dp-demo
      project: roboshop
  # pod definition
  template:
    metadata:
      labels:
        purpose: dp-demo
        project: roboshop
    spec:
      containers:
      - name: nginx
        image: nginx:latest